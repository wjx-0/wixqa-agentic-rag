# 优化的点

## Phase 8 Checker 过度保守

当前发现 Qwen3 8B evidence checker 判断偏激进：很多 baseline top10 已经覆盖全部 gold articles 的样本，仍被模型判为 `sufficient=false`，导致不必要的 second-hop retrieval。

旧版服务器结果中：

```text
source_A_count = 160
source_A_sufficient_rate = 0.0125
source_A_insufficient = 157 / 160
```

这说明 checker 更像是在追求“完全显式、穷尽边界情况”的证据，而不是判断“当前证据是否足够支持一个正确、有用、不误导的回答”。

## 已做优化

将 checker prompt 从“完整性审稿”改为“阻塞性缺口判断”：

```text
sufficient=true:
当前 top10 chunks 已经能支持一个正确、有用、不误导的回答。

sufficient=false:
只有当缺少关键证据会让答案不可靠、实质不完整或误导用户时才触发。
```

新增字段：

```json
{
  "blocking_missing_evidence": [],
  "nice_to_have_missing_evidence": []
}
```

触发 second-hop retrieval 的条件收紧为：

```text
checker_valid
AND sufficient=false
AND blocking_missing_evidence 非空
AND next_queries 非空
```

`nice_to_have_missing_evidence` 只记录非阻塞性补充信息，不触发检索。

## 复跑后重点观察

重新运行 Phase 8 后重点看：

```text
source_A_sufficient_rate 越高越好
source_A_insufficient_count 越低越好
source_A_unnecessary_retrieval_count 越低越好
avg_queries_for_source_A 越低越好
source_C_checker_sufficient_count 仍需接近 0
LLM_C_pool_rescued_count 不应明显下降
multi_LLM_C_pool_rescued_count 不应明显下降
```

理想结果是：减少 A 类样本的无效召回，同时仍然能识别 C 类样本的真实缺证据问题。
